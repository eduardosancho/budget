<%= form_with(model: @category, url: categories_path, method: :post, html: { class: 'category-form-new d-flex flex-column m-4' }, data: { turbo: false }) do |form| %>
  <% if @category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% @category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <h3 class='mb-2 align-self-center'>New Category</h3>
  <div class="mb-3">
    <%= form.label :name %>
    <%= form.text_field :name, placeholder: "Enter the food", class: 'form-control' %>
  </div>
   <div class="mb-3">
    <%= form.label :icon %>
    <%= form.text_field :icon, placeholder: "Enter an icon url", class: 'form-control' %>
  </div>
  <%= form.submit 'Submit', class:"btn btn-primary"%>

<% end %>
